/*SELECT ROUND((AVG(SALARY) - MIN(SALARY)), 2) "Real Amount"
FROM EMPLOYEES*/

/*SELECT DEPARTMENT_ID, 
        MAX(SALARY) "HIGHEST", 
        MIN(SALARY) "LOWEST", 
        ROUND(AVG(SALARY),2) "AVERAGE"
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
ORDER BY 4 DESC;*/

/*SELECT DEPARTMENT_ID "Dept No",
      JOB_ID "Job", 
      COUNT(EMPLOYEE_ID) "No in Dept"
FROM EMPLOYEES 
GROUP BY DEPARTMENT_ID, JOB_ID 
HAVING COUNT(EMPLOYEE_ID) > 1
ORDER BY 3 DESC;*/

/*SELECT  JOB_ID, 
        SUM(SALARY)"TOTAL" 
FROM EMPLOYEES 
WHERE JOB_ID NOT IN ('AD_PRES', 'AD_VP') 
GROUP BY JOB_ID 
HAVING SUM(SALARY) > 15000 
ORDER BY 2 DESC;*/


/*SELECT MANAGER_ID, 
      COUNT(EMPLOYEE_ID) "NO" 
FROM EMPLOYEES 
WHERE MANAGER_ID NOT IN (100,101,102) 
GROUP BY MANAGER_ID 
HAVING COUNT(EMPLOYEE_ID) > 1
ORDER BY 2 DESC;*/

/*SELECT DEPARTMENT_ID, MIN(HIRE_DATE) "EARLIEST", MAX(HIRE_DATE) "LATEST" 
FROM EMPLOYEES 
WHERE DEPARTMENT_ID NOT IN (10,20) 
GROUP BY DEPARTMENT_ID 
HAVING TO_CHAR(MAX(HIRE_DATE),'YYYY') < 2000
ORDER BY 3 DESC;*/

/*SELECT COUNTRY_NAME, LENGTH(COUNTRY_NAME) AS "#Letters" 
FROM COUNTRIES 
ORDER BY 2 DESC;*/

/*SELECT COUNTRY_NAME, REPLACE(COUNTRY_NAME,'a','o') AS "REPLACED" 
FROM COUNTRIES;*/

/*SELECT CURRENT_DATE, NEXT_DAY(CURRENT_DATE + 14,'SUNDAY') "3rd SUNDAY" 
FROM DUAL;*/
